export enum Errors {
  INVALID_EMAIL = 'Invalid email address. Please enter a valid email.',
  INVALID_PASSWORD = 'Password is not strong enough. It must contain at least 8 characters, one uppercase letter, one number, and one special character.',
}
